services:
  frontend:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - .:/app
      - /app/node_modules
    command: ["sh", "-c", "npm install && npx ng serve --host 0.0.0.0 --poll 2000 --disable-host-check"]